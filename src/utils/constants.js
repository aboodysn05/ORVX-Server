// Platform-wide fixed limits.

// Every club runs a fixed 16-player squad; admins cannot change it.
export const SQUAD_CAP = 16;

// Exactly eight official club slots exist.
export const CLUB_SLOTS = 8;
